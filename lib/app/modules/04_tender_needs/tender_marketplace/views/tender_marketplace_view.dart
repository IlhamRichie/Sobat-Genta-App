import 'package:flutter/material.dart';
class TenderMarketplaceView extends StatelessWidget {
  const TenderMarketplaceView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Halaman Kebutuhan (Tab 4)")));
  }
}